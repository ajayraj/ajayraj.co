{
  "name": "astro-site",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "dev:all": "concurrently \"npm run dev\" \"npm run dev --prefix tools/cs-anki\" \"npm run dev --prefix tools/clip\"",
    "build:all": "npm run build && npm run build --prefix tools/cs-anki && npm run build --prefix tools/clip"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.18",
    "astro": "^5.17.1",
    "tailwindcss": "^4.1.18"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
